int main(){
    int number;
    int sum = 0;
    do{
        printf("Enter a number to add on the sum value which is %d: ", sum);
        scanf(" %d", &number);
        sum+=number;
    }
    while(sum<=100);
    return 0;
}
